"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.packageJsonPlugin = void 0;
const node_fs_1 = __importDefault(require("node:fs"));
const fields_constant_1 = require("./constants/fields.constant");
const file_constant_1 = require("./constants/file.constant");
const message_enum_1 = require("./enums/message.enum");
const out_helper_1 = require("./helpers/out.helper");
const package_json_1 = __importDefault(require("@npmcli/package-json"));
const handler = (lifecycle, options) => {
    return async () => {
        const packageJson = await package_json_1.default.load(options.overridePackageJson ?? process.cwd());
        const packageJsonContent = packageJson.content;
        fields_constant_1.shouldRemoveFields.forEach((shouldRemoveField) => {
            // eslint-disable-next-line @typescript-eslint/no-dynamic-delete
            delete packageJsonContent[shouldRemoveField];
        });
        const resolvedOutDir = (0, out_helper_1.resolveOutDir)(options);
        if (!node_fs_1.default.existsSync(resolvedOutDir)) {
            node_fs_1.default.mkdirSync(resolvedOutDir, { recursive: true });
        }
        const resolvedOutFile = `${resolvedOutDir}/${file_constant_1.PACKAGE_JSON_FILENAME}`;
        try {
            node_fs_1.default.writeFileSync(resolvedOutFile, JSON.stringify(packageJsonContent, null, 2));
        }
        catch {
            return {
                errors: [
                    {
                        text: message_enum_1.Messages.PACKAGE_JSON_WRITE
                    }
                ]
            };
        }
        return null;
    };
};
const packageJsonPlugin = (options) => ({
    name: "esbuild-plugin-package-json",
    setup: (build) => {
        const lifecycle = options?.lifecycle ?? "onEnd";
        const resolvePathOptions = {
            overrideOut: options?.overrideOut,
            overridePackageJson: options?.overridePackageJson,
            outBase: build.initialOptions.outbase,
            outDir: build.initialOptions.outdir,
            outFile: build.initialOptions.outfile
        };
        const handlerRef = handler(lifecycle, resolvePathOptions);
        switch (lifecycle) {
            case "onStart":
                build.onStart(handlerRef);
                break;
            case "onEnd":
                build.onEnd(handlerRef);
                break;
        }
    }
});
exports.packageJsonPlugin = packageJsonPlugin;
