"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.fileCopyPlugin = void 0;
const node_path_1 = __importDefault(require("node:path"));
const node_fs_1 = __importDefault(require("node:fs"));
const fast_glob_1 = __importDefault(require("fast-glob"));
const handler = (handlerOptions) => {
    return async () => {
        handlerOptions.inputs?.forEach((input) => {
            const inputFilePath = node_path_1.default.join(process.cwd(), input.from);
            if (!node_fs_1.default.existsSync(inputFilePath))
                return;
            const outputPath = node_path_1.default.join(process.cwd(), input.to);
            const outputPathDir = node_path_1.default.parse(outputPath).dir;
            if (!node_fs_1.default.existsSync(outputPathDir))
                node_fs_1.default.mkdirSync(outputPathDir, { recursive: true });
            node_fs_1.default.copyFileSync(inputFilePath, outputPath);
        });
        handlerOptions.globs?.forEach((glob) => {
            const inputFilePaths = fast_glob_1.default.sync(glob.from);
            inputFilePaths.forEach(((inputFilePath) => {
                if (!node_fs_1.default.existsSync(inputFilePath))
                    return;
                const inputFileName = node_path_1.default.parse(inputFilePath).base;
                const outputFilePath = node_path_1.default.join(process.cwd(), glob.to, inputFileName);
                const outputDirPath = node_path_1.default.parse(outputFilePath).dir;
                if (!node_fs_1.default.existsSync(outputDirPath))
                    node_fs_1.default.mkdirSync(outputDirPath, { recursive: true });
                node_fs_1.default.copyFileSync(inputFilePath, outputFilePath);
            }));
        });
    };
};
const fileCopyPlugin = (options) => ({
    name: "esbuild-plugin-file-copy",
    setup: (build) => {
        const lifecycle = options?.lifecycle ?? "onEnd";
        const handlerOptions = {
            inputs: options?.inputs,
            globs: options?.globs
        };
        const handlerRef = handler(handlerOptions);
        switch (lifecycle) {
            case "onStart":
                build.onStart(handlerRef);
                break;
            case "onEnd":
                build.onEnd(handlerRef);
                break;
        }
    }
});
exports.fileCopyPlugin = fileCopyPlugin;
