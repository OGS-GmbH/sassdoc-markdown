// src/builder/footnote.ts
function footnoteLink(index) {
  return {
    toString() {
      return `[^${index}]`;
    }
  };
}
function footnote(index, value) {
  return {
    toString() {
      return `[^${index}]: ${value.toString()}`;
    }
  };
}
export {
  footnote,
  footnoteLink
};
