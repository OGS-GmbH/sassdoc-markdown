// src/builder/builder.ts
function define(...nodes) {
  return {
    toString() {
      let value = "";
      for (const node of nodes) {
        if (node === null)
          continue;
        value += node.toString();
      }
      return value;
    }
  };
}
export {
  define
};
