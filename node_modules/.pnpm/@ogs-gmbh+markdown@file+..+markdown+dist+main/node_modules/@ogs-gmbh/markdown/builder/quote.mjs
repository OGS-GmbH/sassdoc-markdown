// src/builder/quote.ts
var LEADING = ">";
function quote(nodes) {
  return {
    toString() {
      let data = "";
      for (const node of nodes)
        data += `${LEADING} ${node.toString()}`;
      return data;
    }
  };
}
export {
  quote
};
