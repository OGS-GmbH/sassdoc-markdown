// src/builder/container.ts
var CONTAINER_SEP = ":::";
function container(type, value, title) {
  return {
    toString() {
      let data = `${CONTAINER_SEP} ${type}`;
      if (title !== void 0)
        data += ` ${title}`;
      data += "\n";
      data += value.toString();
      data += "\n";
      data += CONTAINER_SEP;
      return data;
    }
  };
}
export {
  container
};
