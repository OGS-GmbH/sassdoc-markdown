// src/builder/paragraph.ts
var TEXT_STYLE_TAG = {
  bold: "**",
  boldAlt: "__",
  italic: "*",
  italicAlt: "_",
  strikethrough: "~~",
  strikethroughAlt: "~",
  allBoldAndItalic: "***",
  subscript: {
    open: "<sub>",
    close: "</sub>"
  },
  superscript: {
    open: "<sup>",
    close: "</sup>"
  },
  underline: {
    open: "<ins>",
    close: "</ins>"
  }
};
function paragraph(value, options) {
  return {
    toString() {
      if (options === void 0)
        return value.toString();
      let opening = "";
      let closing = "";
      for (const option of options) {
        const tag = TEXT_STYLE_TAG[option.toString()];
        if (typeof tag === "string") {
          opening += tag;
          closing = tag + closing;
          continue;
        }
        opening += tag.open;
        closing = tag.close + closing;
      }
      return `${opening}${value.toString()}${closing}`;
    }
  };
}
export {
  paragraph
};
