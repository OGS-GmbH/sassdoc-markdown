// src/builder/task.ts
var CHECKED = "x";
function task(checked, node) {
  return {
    toString() {
      const char = checked ? CHECKED : " ";
      return `[${char}] ${node.toString()}`;
    }
  };
}
export {
  task
};
